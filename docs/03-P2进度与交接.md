# 03 · P2 进度与交接（2026-08-02）

> 换电脑交接说明：所有代码已推送到 GitHub（main @ 53784b3），数据库迁移 0001–0009 已全部应用到 Supabase 云项目。

## 已完成的 P2 里程碑（均已本地验证）

| 里程碑 | 内容 | 状态 |
|--------|------|------|
| M1 互动地基 | follows 表 + 关注/取关 + 首页关注 Tab + 主页关注数/粉丝数 + 收藏/喜欢 Tab | ✅ 已验证 |
| M2 通知中心 | notifications 表 + 点赞/评论/关注自动触发 + 通知页 + 未读角标 + 打开即已读 | ✅ 已验证 |
| M3 搜索 | pg_trgm 中文模糊检索 + /search 页（内容/用户/话题/猫咪/品种） | ✅ 已验证 |
| M4 评论升级+内容管理 | 楼中楼回复 + 评论点赞 + 草稿箱 + 笔记编辑（重新送审） | ✅ 已验证 |
| M5 推荐热度 | hot_score = 赞×3+评×5+藏×4，推荐 Tab 热度排序 | ✅ 已验证 |
| M6 后台运营（schema） | reports / sensitive_words / breeds 表 + moderator 角色 + is_moderator() / has_sensitive_word() | ✅ 迁移已应用 |

## M6 剩余工作（新电脑继续）

### 后台（/admin）
- [ ] 举报处理页：工单列表 + 处置（删除内容 / 封禁用户 / 驳回举报）
- [ ] 内容管理页：已发布内容检索、下架、删除
- [ ] 数据统计：仪表盘升级（发布趋势、热门内容、活跃用户、品类分布）
- [ ] 品种管理页：breeds CRUD（增/改/启停用）
- [ ] 敏感词管理页：敏感词增删启停

### 前台
- [ ] 笔记/评论举报按钮（reports 表插入，RLS 已就绪）
- [ ] 发布/评论接入敏感词校验（has_sensitive_word() 已就绪）
- [ ] 可选：CAT_BREEDS 常量改为从 breeds 表读取

## 技术要点备忘
- 迁移脚本：`node scripts/db-setup.mjs`（自动发现 supabase/migrations/*.sql，每个迁移独立事务）
- 单独应用迁移：直接编辑后用 db-setup 即可；已有结构会幂等跳过
- i18n 新增词条后：`node scripts/supplement-dictionaries.mjs`（自动补翻 10 种语言）
- 校验词典：`node scripts/verify-dictionaries.mjs`
- 注意：不要并行跑词典生成；不要用 PowerShell 读写含中文的 UTF-8 文件
- 通知实时性：当前为落库 + 刷新拉取；WebSocket 实时推送后置（P3）
- 角色：admin（超管）/ moderator（审核员）/ user；后台登录走 env 的 ADMIN_USERNAME/ADMIN_PASSWORD

## 换电脑后启动步骤
1. `git clone git@github.com:rappizo/catsjust.git`
2. 从旧电脑拷贝 `.env.local`（含 Supabase/DB/Admin/APIYI/deepseek 密钥，不入库）
3. `npm install`
4. `npm run dev`（本地）或 `vercel --prod --yes`（部署）
